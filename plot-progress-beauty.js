// ==DouluoLockedRegex==
// @name         [剧情推进]斗罗大陆 I-IV · Soul Land 前置审查面板 @0.6
// @module       plot-progress-beauty
// @version      @0.6
// @build        deterministic
// @sourceHash   sha256:a584f07a0461ae19b6154773d4d78ce7c6e4e95afe5439c65e53d9d48e7041c7
// ==/DouluoLockedRegex==
(function () {
  "use strict";

  const MODULE_NAME = "plot-progress-beauty";
  const ROOT_ID = "dlgh-plot-progress-beauty";
  const REPLACE_STRING = "```html\n<!DOCTYPE html>\n<html lang=\"zh-CN\">\n<head>\n<meta charset=\"UTF-8\" />\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n<title>Douluo Plot Progress Panel</title>\n<style>\n:root {\n  color-scheme: dark;\n}\n\n* {\n  box-sizing: border-box;\n}\n\nhtml,\nbody {\n  margin: 0;\n  padding: 0;\n  background: transparent;\n}\n\nbody {\n  font: 14px/1.65 \"Microsoft YaHei\", \"Noto Sans SC\", system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  letter-spacing: 0;\n  color: #edf6ff;\n}\n\n.dpp-root {\n  width: min(100%, 900px);\n  margin: 12px auto;\n  padding: 0;\n  background: transparent;\n}\n\n.dpp-shell {\n  position: relative;\n  overflow: hidden;\n  border: 1px solid rgba(94, 231, 255, .24);\n  border-radius: 8px;\n  background:\n    linear-gradient(135deg, rgba(94, 231, 255, .10), transparent 38%),\n    linear-gradient(180deg, rgba(232, 195, 108, .07), transparent 34%),\n    rgba(4, 12, 28, .84);\n  box-shadow:\n    0 18px 45px rgba(0, 0, 0, .38),\n    inset 0 1px 0 rgba(255, 255, 255, .08);\n  backdrop-filter: blur(16px);\n}\n\n.dpp-shell::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  background:\n    linear-gradient(90deg, rgba(94, 231, 255, .10) 1px, transparent 1px),\n    linear-gradient(180deg, rgba(94, 231, 255, .07) 1px, transparent 1px);\n  background-size: 38px 38px;\n  mask-image: linear-gradient(180deg, rgba(0, 0, 0, .75), transparent 72%);\n}\n\n.dpp-player,\n.dpp-groups {\n  position: relative;\n  z-index: 1;\n}\n\n.dpp-player {\n  margin: 16px 16px 10px;\n  padding: 13px 14px;\n  border: 1px solid rgba(232, 195, 108, .26);\n  border-radius: 8px;\n  background: rgba(8, 22, 48, .70);\n}\n\n.dpp-player-body,\n.dpp-card-body {\n  margin: 0;\n  white-space: pre-wrap;\n  overflow-wrap: anywhere;\n  word-break: break-word;\n  color: #edf6ff;\n  font: inherit;\n}\n\n.dpp-player-body {\n  color: #f4f8ff;\n}\n\n.dpp-groups {\n  display: grid;\n  gap: 12px;\n  padding: 0 16px 16px;\n}\n\n.dpp-entry {\n  position: relative;\n  overflow: hidden;\n  min-width: 0;\n  min-height: 58px;\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 12px;\n  padding: 11px 14px;\n  border: 1px solid rgba(94, 231, 255, .24);\n  border-radius: 8px;\n  background:\n    linear-gradient(135deg, rgba(94, 231, 255, .13), transparent 46%),\n    linear-gradient(90deg, rgba(232, 195, 108, .12), transparent 38%),\n    rgba(7, 18, 40, .64);\n  color: #edf6ff;\n  font: inherit;\n  cursor: pointer;\n  text-align: left;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, .08),\n    0 0 0 1px rgba(4, 12, 28, .20);\n}\n\n.dpp-entry::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  background:\n    linear-gradient(90deg, transparent, rgba(94, 231, 255, .16), transparent),\n    repeating-linear-gradient(90deg, transparent 0 34px, rgba(94, 231, 255, .05) 34px 35px);\n  opacity: .42;\n}\n\n.dpp-entry:hover,\n.dpp-entry.is-open {\n  border-color: rgba(94, 231, 255, .42);\n  background:\n    linear-gradient(135deg, rgba(94, 231, 255, .18), transparent 54%),\n    linear-gradient(90deg, rgba(232, 195, 108, .16), transparent 42%),\n    rgba(8, 22, 48, .84);\n}\n\n.dpp-entry-mark,\n.dpp-entry-title,\n.dpp-entry-count {\n  position: relative;\n  z-index: 1;\n}\n\n.dpp-entry-mark {\n  width: 40px;\n  height: 40px;\n  display: block;\n  border: 1px solid rgba(232, 195, 108, .42);\n  border-radius: 50%;\n  background:\n    radial-gradient(circle at 50% 50%, rgba(232, 195, 108, .92) 0 3px, transparent 4px),\n    radial-gradient(circle at 50% 50%, rgba(94, 231, 255, .18), transparent 58%),\n    rgba(232, 195, 108, .06);\n  box-shadow:\n    0 0 20px rgba(94, 231, 255, .16),\n    inset 0 0 16px rgba(94, 231, 255, .08);\n}\n\n.dpp-entry-mark::before,\n.dpp-entry-mark::after {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border: 1px solid rgba(94, 231, 255, .54);\n  box-shadow: 0 0 12px rgba(94, 231, 255, .20);\n  animation: dppSoulRingSpin 7s linear infinite;\n}\n\n.dpp-entry-mark::before {\n  transform: rotateX(64deg) rotateZ(18deg);\n}\n\n.dpp-entry-mark::after {\n  inset: 8px;\n  border-color: rgba(232, 195, 108, .66);\n  animation-duration: 9s;\n  animation-direction: reverse;\n  transform: rotateX(64deg) rotateZ(-32deg);\n}\n\n.dpp-entry-mark i {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid rgba(94, 231, 255, .30);\n  border-radius: 50%;\n  background: transparent;\n  box-shadow: none;\n  animation: dppSoulRingSpin 11s linear infinite;\n}\n\n.dpp-entry-mark i:nth-child(1) {\n  transform: rotateX(58deg) rotateZ(72deg);\n}\n\n.dpp-entry-mark i:nth-child(2) {\n  inset: 10px;\n  border-color: rgba(232, 195, 108, .42);\n  animation-duration: 6s;\n  transform: rotateX(62deg) rotateZ(132deg);\n}\n\n.dpp-entry-mark i:nth-child(3) {\n  inset: 14px;\n  border-color: rgba(94, 231, 255, .42);\n  animation-duration: 8s;\n  animation-direction: reverse;\n  transform: rotateX(68deg) rotateZ(12deg);\n}\n\n.dpp-entry-mark i:nth-child(4) {\n  display: none;\n}\n\n@keyframes dppSoulRingSpin {\n  from {\n    rotate: 0deg;\n  }\n\n  to {\n    rotate: 360deg;\n  }\n}\n\n.dpp-entry-title {\n  min-width: 0;\n  color: #f8fbff;\n  font-size: 15px;\n  font-weight: 700;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.dpp-entry-count {\n  color: #e8c36c;\n  font-size: 12px;\n  font-weight: 700;\n}\n\n.dpp-audit-body {\n  display: grid;\n  gap: 12px;\n}\n\n.dpp-audit-body[hidden] {\n  display: none;\n}\n\n.dpp-tabs {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: 8px;\n}\n\n.dpp-tab {\n  min-width: 0;\n  min-height: 44px;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n  padding: 9px 11px;\n  border: 1px solid rgba(94, 231, 255, .20);\n  border-radius: 8px;\n  background: rgba(7, 18, 40, .62);\n  color: #edf6ff;\n  font: inherit;\n  cursor: pointer;\n  text-align: left;\n}\n\n.dpp-tab:hover,\n.dpp-tab.is-active {\n  border-color: rgba(94, 231, 255, .36);\n  background:\n    linear-gradient(135deg, rgba(94, 231, 255, .15), transparent 70%),\n    rgba(8, 22, 48, .82);\n  box-shadow: inset 0 1px 0 rgba(255, 255, 255, .08);\n}\n\n.dpp-tab.is-active {\n  color: #f8fbff;\n}\n\n.dpp-tab span {\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  font-size: 14px;\n  font-weight: 700;\n}\n\n.dpp-tab small {\n  flex: 0 0 auto;\n  color: rgba(156, 173, 198, .86);\n  font-size: 12px;\n  font-weight: 700;\n}\n\n.dpp-tab.is-active small {\n  color: #e8c36c;\n}\n\n.dpp-panes {\n  display: none;\n  border: 1px solid rgba(94, 231, 255, .24);\n  border-radius: 8px;\n  background: rgba(7, 18, 40, .62);\n  padding: 12px;\n}\n\n.dpp-panes.has-active-pane {\n  display: block;\n}\n\n.dpp-pane {\n  display: none;\n  gap: 8px;\n}\n\n.dpp-pane.is-active {\n  display: grid;\n}\n\n.dpp-card {\n  padding: 10px;\n  border: 1px solid rgba(255, 255, 255, .08);\n  border-radius: 8px;\n  background: rgba(255, 255, 255, .035);\n}\n\n.dpp-card-title {\n  margin-bottom: 6px;\n  color: #5ee7ff;\n  font-size: 12px;\n  font-weight: 700;\n}\n\n.dpp-card-body {\n  color: rgba(237, 246, 255, .92);\n  font-size: 13px;\n}\n\n.dpp-am-card {\n  border-color: rgba(232, 195, 108, .20);\n}\n\n.dpp-am-list {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n}\n\n.dpp-am-chip {\n  display: inline-flex;\n  align-items: center;\n  min-height: 28px;\n  padding: 4px 9px;\n  border: 1px solid rgba(232, 195, 108, .34);\n  border-radius: 8px;\n  color: #f5d884;\n  background: rgba(232, 195, 108, .08);\n  font: inherit;\n  font-size: 12px;\n  font-weight: 700;\n  letter-spacing: 0;\n  cursor: pointer;\n}\n\n.dpp-am-chip:hover,\n.dpp-am-chip.is-active {\n  border-color: rgba(232, 195, 108, .70);\n  background:\n    linear-gradient(135deg, rgba(232, 195, 108, .20), rgba(94, 231, 255, .08)),\n    rgba(232, 195, 108, .09);\n  box-shadow: 0 0 18px rgba(232, 195, 108, .10);\n}\n\n.dpp-memo-view {\n  min-height: 58px;\n  padding: 10px;\n  border: 1px solid rgba(232, 195, 108, .20);\n  border-radius: 8px;\n  background:\n    linear-gradient(135deg, rgba(232, 195, 108, .06), transparent 70%),\n    rgba(255, 255, 255, .035);\n  color: rgba(237, 246, 255, .92);\n  overflow-wrap: anywhere;\n  word-break: break-word;\n}\n\n.dpp-memo-view.is-empty,\n.dpp-memo-view.is-loading,\n.dpp-memo-view.is-warning {\n  display: grid;\n  align-items: center;\n  color: rgba(156, 173, 198, .82);\n  font-size: 13px;\n}\n\n.dpp-memo-view.is-warning {\n  border-color: rgba(232, 195, 108, .36);\n  color: #f5d884;\n}\n\n.dpp-memo-head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n  margin-bottom: 8px;\n  color: #5ee7ff;\n  font-size: 12px;\n  font-weight: 700;\n}\n\n.dpp-memo-head b {\n  color: #f5d884;\n  font-size: 13px;\n}\n\n.dpp-memo-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr);\n  gap: 8px;\n}\n\n.dpp-memo-field {\n  display: grid;\n  gap: 4px;\n  padding: 8px;\n  border: 1px solid rgba(255, 255, 255, .07);\n  border-radius: 8px;\n  background: rgba(4, 12, 28, .30);\n}\n\n.dpp-memo-label {\n  color: #e8c36c;\n  font-size: 11px;\n  font-weight: 700;\n}\n\n.dpp-memo-value {\n  margin: 0;\n  white-space: pre-wrap;\n  overflow-wrap: anywhere;\n  word-break: break-word;\n  color: rgba(237, 246, 255, .92);\n  font: inherit;\n  font-size: 13px;\n}\n\n.dpp-memo-empty {\n  color: rgba(156, 173, 198, .75);\n  font-size: 13px;\n}\n\n.dpp-am-empty {\n  color: rgba(156, 173, 198, .75);\n  font-size: 13px;\n}\n\n.dpp-empty {\n  color: rgba(156, 173, 198, .75);\n  font-style: italic;\n}\n\n@media (max-width: 520px) {\n  .dpp-root {\n    margin: 8px auto;\n  }\n\n  .dpp-player {\n    margin: 12px 10px 8px;\n  }\n\n  .dpp-groups {\n    padding: 0 10px 12px;\n  }\n\n  .dpp-entry {\n    min-height: 52px;\n    padding: 9px 10px;\n  }\n\n  .dpp-tabs {\n    grid-template-columns: repeat(2, minmax(0, 1fr));\n  }\n\n  .dpp-panes {\n    padding: 10px;\n  }\n}\n\n</style>\n</head>\n<body>\n<section class=\"dpp-root\" data-root data-dpp-root>\n  <template data-dpp-raw>$1</template>\n  <div class=\"dpp-shell\" data-dpp-panel hidden>\n    <section class=\"dpp-player\">\n      <pre class=\"dpp-player-body\" data-dpp-player></pre>\n    </section>\n\n    <div class=\"dpp-groups\" data-dpp-groups></div>\n  </div>\n</section>\n<script>\n(function () {\n  \"use strict\";\n\n  const TABS = [\n    {\n      key: \"recall\",\n      title: \"Recall\",\n      tags: [\"chapter_state\", \"anchor_update\", \"recall_detail\", \"recall\"],\n    },\n    {\n      key: \"time\",\n      title: \"时间推进\",\n      tags: [\"time_state\", \"scene_state\", \"runtime_state\", \"time_recall\"],\n    },\n    {\n      key: \"writing\",\n      title: \"正文\",\n      tags: [\"writing_directive\", \"format_directive\", \"db_delta\"],\n    },\n    {\n      key: \"verdict\",\n      title: \"合理性\",\n      tags: [\"rationality_verdict\", \"butterfly_delta\"],\n    },\n  ];\n\n  const TAG_LABELS = {\n    chapter_state: \"章节锚点\",\n    anchor_update: \"锚点更新\",\n    recall_detail: \"召回依据\",\n    recall: \"AM 短键\",\n    time_state: \"时间状态\",\n    scene_state: \"场景状态\",\n    runtime_state: \"运行态监控\",\n    time_recall: \"时间召回\",\n    rationality_verdict: \"合理性裁定\",\n    butterfly_delta: \"蝴蝶效应\",\n    writing_directive: \"正文指令\",\n    format_directive: \"格式交接\",\n    db_delta: \"数据库门控\",\n  };\n\n  const MEMO_TABLE_UID = \"sheet_3NoMc1wI\";\n  const MEMO_TABLE_NAMES = [\"纪要表\", \"chronicle\", MEMO_TABLE_UID];\n  const MEMO_FIELDS = [\"时间跨度\", \"地点\", \"纪要类型\", \"纪要\", \"概览\", \"关联人物\", \"待办/线索\", \"编码索引\"];\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function decodeHtml(value) {\n    const textarea = document.createElement(\"textarea\");\n    textarea.innerHTML = String(value || \"\");\n    return textarea.value;\n  }\n\n  function normalizeRaw(value) {\n    let text = String(value || \"\")\n      .replace(/<!--[\\s\\S]*?-->/g, \"\")\n      .replace(/<br\\s*\\/?>/gi, \"\\n\")\n      .trim();\n\n    for (let i = 0; i < 3; i += 1) {\n      const decoded = decodeHtml(text);\n      if (decoded === text) break;\n      text = decoded;\n    }\n\n    return text.trim();\n  }\n\n  function extractTag(source, tagName) {\n    const blocks = [];\n    const re = new RegExp(\"<\" + tagName + \"\\\\b[^>]*>([\\\\s\\\\S]*?)<\\\\/\" + tagName + \">\", \"gi\");\n    let match;\n    while ((match = re.exec(source))) {\n      const text = normalizeRaw(match[1]);\n      if (text) blocks.push(text);\n    }\n    return blocks.join(\"\\n\\n\").trim();\n  }\n\n  function extractAmCodes() {\n    const seen = new Set();\n    const codes = [];\n    for (const value of arguments) {\n      String(value || \"\").replace(/\\bAM\\d{3,}\\b/gi, (code) => {\n        const normalized = code.toUpperCase();\n        if (!seen.has(normalized)) {\n          seen.add(normalized);\n          codes.push(normalized);\n        }\n        return code;\n      });\n    }\n    return codes;\n  }\n\n  function hosts() {\n    const list = [window];\n    try {\n      if (window.parent && window.parent !== window) list.push(window.parent);\n    } catch (_) {}\n    try {\n      if (window.top && !list.includes(window.top)) list.push(window.top);\n    } catch (_) {}\n    return list;\n  }\n\n  function getGlobal(name) {\n    for (const host of hosts()) {\n      try {\n        if (host && host[name]) return host[name];\n      } catch (_) {}\n    }\n    return null;\n  }\n\n  function parseMaybeJson(value) {\n    if (typeof value !== \"string\") return value;\n    try {\n      return JSON.parse(value);\n    } catch (_) {\n      return null;\n    }\n  }\n\n  async function readDatabase() {\n    const api = getGlobal(\"AutoCardUpdaterAPI\");\n    if (!api) throw new Error(\"未检测到 AutoCardUpdaterAPI\");\n\n    const wholeDatabaseCalls = [\"exportTableAsJson\", \"exportDataAsJson\", \"getDatabase\", \"getAllTables\"];\n    let lastError = null;\n    for (const name of wholeDatabaseCalls) {\n      if (typeof api[name] !== \"function\") continue;\n      try {\n        const result = parseMaybeJson(await api[name]());\n        if (result && typeof result === \"object\") return result;\n      } catch (error) {\n        lastError = error;\n      }\n    }\n\n    const tableCalls = [\"getTable\", \"getTableData\", \"getRows\"];\n    for (const callName of tableCalls) {\n      if (typeof api[callName] !== \"function\") continue;\n      for (const tableName of MEMO_TABLE_NAMES) {\n        try {\n          const result = parseMaybeJson(await api[callName](tableName));\n          if (result && typeof result === \"object\") return { [tableName]: result };\n        } catch (error) {\n          lastError = error;\n        }\n      }\n    }\n\n    if (lastError) throw lastError;\n    throw new Error(\"未检测到可用的数据库读取接口\");\n  }\n\n  function sheetNames(key, sheet) {\n    const names = [key];\n    if (sheet && typeof sheet === \"object\") {\n      names.push(sheet.name, sheet.title, sheet.tableName, sheet.uid, sheet.id, sheet.key);\n    }\n    return names.map((name) => String(name || \"\").trim()).filter(Boolean);\n  }\n\n  function headersFromSheet(sheet) {\n    if (!sheet) return [];\n    const source = Array.isArray(sheet) ? sheet : (sheet.content || sheet.rows || sheet.data || sheet.values);\n    if (!Array.isArray(source) || !source.length || !Array.isArray(source[0])) return [];\n    return source[0].map((cell) => String(cell == null ? \"\" : cell).trim());\n  }\n\n  function hasMemoHeaders(sheet) {\n    const headers = headersFromSheet(sheet);\n    return MEMO_FIELDS.filter((field) => headers.includes(field)).length >= 3;\n  }\n\n  function findMemoSheet(database) {\n    const db = parseMaybeJson(database);\n    if (!db || typeof db !== \"object\") return null;\n\n    if (Array.isArray(db)) {\n      for (const sheet of db) {\n        const names = sheetNames(\"\", sheet);\n        if (names.some((name) => MEMO_TABLE_NAMES.includes(name)) || hasMemoHeaders(sheet)) {\n          return { key: names[0] || MEMO_TABLE_UID, sheet };\n        }\n      }\n      return null;\n    }\n\n    if (db[MEMO_TABLE_UID]) return { key: MEMO_TABLE_UID, sheet: db[MEMO_TABLE_UID] };\n\n    for (const [key, sheet] of Object.entries(db)) {\n      const names = sheetNames(key, sheet);\n      if (names.some((name) => MEMO_TABLE_NAMES.includes(name)) || hasMemoHeaders(sheet)) {\n        return { key, sheet };\n      }\n    }\n    return null;\n  }\n\n  function objectFromRow(headers, row) {\n    const item = {};\n    headers.forEach((header, index) => {\n      if (!header) return;\n      item[header] = Array.isArray(row) ? row[index] : row && row[header];\n    });\n    return item;\n  }\n\n  function normalizeRows(sheet) {\n    if (!sheet) return [];\n    const source = Array.isArray(sheet) ? sheet : (sheet.content || sheet.rows || sheet.data || sheet.values);\n    if (!Array.isArray(source)) return [];\n    if (!source.length) return [];\n\n    if (Array.isArray(source[0])) {\n      const headers = source[0].map((cell) => String(cell == null ? \"\" : cell).trim());\n      const firstRowLooksLikeHeader = headers.some((header) => MEMO_FIELDS.includes(header) || header === \"row_id\");\n      if (firstRowLooksLikeHeader) {\n        return source.slice(1).map((row) => objectFromRow(headers, row));\n      }\n      return source.map((row, index) => ({\n        row_id: index + 1,\n        纪要: Array.isArray(row) ? row.join(\" / \") : row,\n      }));\n    }\n\n    return source\n      .filter((row) => row && typeof row === \"object\")\n      .map((row) => Object.assign({}, row));\n  }\n\n  function fieldValue(row, names) {\n    for (const name of names) {\n      const value = row && row[name];\n      if (value != null && String(value).trim()) return value;\n    }\n    return \"\";\n  }\n\n  function rowCodeText(row) {\n    return String(fieldValue(row, [\"编码索引\", \"AM\", \"am\", \"code\", \"Code\", \"编码\", \"索引\"]) || Object.values(row || {}).join(\" \"));\n  }\n\n  function rowMatchesCode(row, code) {\n    const normalized = String(code || \"\").toUpperCase();\n    const source = rowCodeText(row).toUpperCase();\n    return new RegExp(\"\\\\b\" + normalized.replace(/[.*+?^$${}()|[\\]\\\\]/g, \"\\\\$$&\") + \"\\\\b\").test(source) || source.includes(normalized);\n  }\n\n  function memoRowHtml(code, row) {\n    const fields = MEMO_FIELDS.map((field) => {\n      const value = row && row[field];\n      if (value == null || String(value).trim() === \"\") return \"\";\n      return [\n        '<div class=\"dpp-memo-field\">',\n        '<div class=\"dpp-memo-label\">' + escapeHtml(field) + \"</div>\",\n        '<pre class=\"dpp-memo-value\">' + escapeHtml(value) + \"</pre>\",\n        \"</div>\",\n      ].join(\"\");\n    }).join(\"\");\n\n    return [\n      '<div class=\"dpp-memo-head\">',\n      '<span>纪要表</span>',\n      '<b>' + escapeHtml(code) + \"</b>\",\n      \"</div>\",\n      '<div class=\"dpp-memo-grid\">',\n      fields || '<div class=\"dpp-memo-empty\">该行没有可显示字段。</div>',\n      \"</div>\",\n    ].join(\"\");\n  }\n\n  function setMemoStatus(view, status, text) {\n    view.className = \"dpp-memo-view \" + status;\n    view.textContent = text;\n  }\n\n  async function loadMemo(root, code) {\n    const view = root.querySelector(\"[data-dpp-memo-view]\");\n    if (!view) return;\n\n    root.querySelectorAll(\"[data-dpp-am-code]\").forEach((button) => {\n      const active = button.getAttribute(\"data-dpp-am-code\") === code;\n      button.classList.toggle(\"is-active\", active);\n      button.setAttribute(\"aria-pressed\", active ? \"true\" : \"false\");\n    });\n\n    setMemoStatus(view, \"is-loading\", \"正在读取纪要表 \" + code + \"...\");\n\n    try {\n      const database = await readDatabase();\n      const memoSheet = findMemoSheet(database);\n      if (!memoSheet) {\n        setMemoStatus(view, \"is-warning\", \"数据库中未找到纪要表。\");\n        return;\n      }\n\n      const row = normalizeRows(memoSheet.sheet).find((item) => rowMatchesCode(item, code));\n      if (!row) {\n        setMemoStatus(view, \"is-warning\", \"纪要表未找到 \" + code + \"。\");\n        return;\n      }\n\n      view.className = \"dpp-memo-view is-loaded\";\n      view.innerHTML = memoRowHtml(code, row);\n    } catch (error) {\n      setMemoStatus(view, \"is-warning\", \"纪要表读取失败：\" + (error && error.message ? error.message : \"未知错误\"));\n    }\n  }\n\n  function renderTag(tagName, body) {\n    const text = body || \"未输出\";\n    const emptyClass = body ? \"\" : \" dpp-empty\";\n    return [\n      '<section class=\"dpp-card\" data-dpp-tag=\"' + escapeHtml(tagName) + '\">',\n      '<div class=\"dpp-card-title\">' + escapeHtml(TAG_LABELS[tagName] || tagName) + '</div>',\n      '<pre class=\"dpp-card-body' + emptyClass + '\">' + escapeHtml(text) + '</pre>',\n      \"</section>\",\n    ].join(\"\");\n  }\n\n  function renderAmStrip(tagMap) {\n    const codes = extractAmCodes(tagMap.recall, tagMap.recall_detail, tagMap.time_recall);\n    if (!codes.length) {\n      return [\n        '<section class=\"dpp-card dpp-am-card\">',\n        '<div class=\"dpp-card-title\">AM 短键</div>',\n        '<div class=\"dpp-am-empty\">未召回稳定 AM 编码</div>',\n        \"</section>\",\n      ].join(\"\");\n    }\n\n    return [\n      '<section class=\"dpp-card dpp-am-card\">',\n      '<div class=\"dpp-card-title\">AM 短键</div>',\n      '<div class=\"dpp-am-list\">',\n      codes.map((code) => [\n        '<button class=\"dpp-am-chip\" type=\"button\" data-dpp-am-code=\"' + escapeHtml(code) + '\" aria-pressed=\"false\">',\n        escapeHtml(code),\n        \"</button>\",\n      ].join(\"\")).join(\"\"),\n      \"</div>\",\n      \"</section>\",\n      '<section class=\"dpp-memo-view is-empty\" data-dpp-memo-view>请选择 AM 短键读取纪要表内容。</section>',\n    ].join(\"\");\n  }\n\n  function renderRecallPanel(tab, tagMap) {\n    return [\n      '<section class=\"dpp-pane\" data-dpp-pane=\"' + escapeHtml(tab.key) + '\">',\n      renderAmStrip(tagMap),\n      renderTag(\"recall_detail\", tagMap.recall_detail),\n      renderTag(\"chapter_state\", tagMap.chapter_state),\n      renderTag(\"anchor_update\", tagMap.anchor_update),\n      \"</section>\",\n    ].join(\"\");\n  }\n\n  function renderPanel(tab, tagMap) {\n    if (tab.key === \"recall\") return renderRecallPanel(tab, tagMap);\n    return [\n      '<section class=\"dpp-pane\" data-dpp-pane=\"' + escapeHtml(tab.key) + '\">',\n      tab.tags.map((tagName) => renderTag(tagName, tagMap[tagName])).join(\"\"),\n      \"</section>\",\n    ].join(\"\");\n  }\n\n  function renderTabs(tagMap) {\n    const totalPresent = TABS.reduce((count, tab) => {\n      return count + tab.tags.reduce((tagCount, tagName) => tagCount + (tagMap[tagName] ? 1 : 0), 0);\n    }, 0);\n    const totalTags = TABS.reduce((count, tab) => count + tab.tags.length, 0);\n\n    return [\n      '<button class=\"dpp-entry\" type=\"button\" data-dpp-audit-entry aria-expanded=\"false\">',\n      '<span class=\"dpp-entry-mark\" aria-hidden=\"true\">',\n      '<i></i><i></i><i></i><i></i>',\n      \"</span>\",\n      '<span class=\"dpp-entry-title\">系统信息</span>',\n      '<span class=\"dpp-entry-count\">' + totalPresent + \"/\" + totalTags + \"</span>\",\n      \"</button>\",\n      '<div class=\"dpp-audit-body\" data-dpp-audit-body hidden>',\n      '<div class=\"dpp-tabs\" role=\"tablist\" aria-label=\"行动分组\">',\n      TABS.map((tab) => {\n        const presentCount = tab.tags.reduce((count, tagName) => count + (tagMap[tagName] ? 1 : 0), 0);\n        return [\n          '<button class=\"dpp-tab\" type=\"button\" role=\"tab\"',\n          ' data-dpp-tab=\"' + escapeHtml(tab.key) + '\"',\n          ' aria-selected=\"false\" aria-expanded=\"false\">',\n          '<span>' + escapeHtml(tab.title) + \"</span>\",\n          '<small>' + presentCount + \"/\" + tab.tags.length + \"</small>\",\n          \"</button>\",\n        ].join(\"\");\n      }).join(\"\"),\n      \"</div>\",\n      '<div class=\"dpp-panes\" aria-live=\"polite\">',\n      TABS.map((tab) => renderPanel(tab, tagMap)).join(\"\"),\n      \"</div>\",\n      \"</div>\",\n    ].join(\"\");\n  }\n\n  function closeTabs(root) {\n    root.querySelectorAll(\"[data-dpp-tab]\").forEach((button) => {\n      button.classList.remove(\"is-active\");\n      button.setAttribute(\"aria-selected\", \"false\");\n      button.setAttribute(\"aria-expanded\", \"false\");\n    });\n    root.querySelectorAll(\"[data-dpp-pane]\").forEach((pane) => {\n      pane.classList.remove(\"is-active\");\n    });\n    const panes = root.querySelector(\".dpp-panes\");\n    if (panes) panes.classList.remove(\"has-active-pane\");\n  }\n\n  function toggleAuditBody(root) {\n    const entry = root.querySelector(\"[data-dpp-audit-entry]\");\n    const body = root.querySelector(\"[data-dpp-audit-body]\");\n    if (!entry || !body) return;\n\n    const willOpen = body.hidden;\n    body.hidden = !willOpen;\n    entry.classList.toggle(\"is-open\", willOpen);\n    entry.setAttribute(\"aria-expanded\", willOpen ? \"true\" : \"false\");\n    if (!willOpen) closeTabs(root);\n  }\n\n  function toggleTab(root, key) {\n    const wasActive = !!root.querySelector('[data-dpp-tab=\"' + key + '\"].is-active');\n    const nextKey = wasActive ? \"\" : key;\n    const hasActive = !!nextKey;\n\n    root.querySelectorAll(\"[data-dpp-tab]\").forEach((button) => {\n      const active = button.getAttribute(\"data-dpp-tab\") === nextKey;\n      button.classList.toggle(\"is-active\", active);\n      button.setAttribute(\"aria-selected\", active ? \"true\" : \"false\");\n      button.setAttribute(\"aria-expanded\", active ? \"true\" : \"false\");\n    });\n    root.querySelectorAll(\"[data-dpp-pane]\").forEach((pane) => {\n      pane.classList.toggle(\"is-active\", pane.getAttribute(\"data-dpp-pane\") === nextKey);\n    });\n    const panes = root.querySelector(\".dpp-panes\");\n    if (panes) panes.classList.toggle(\"has-active-pane\", hasActive);\n  }\n\n  function mount(root) {\n    if (!root || root.dataset.dppMounted === \"1\") return;\n    root.dataset.dppMounted = \"1\";\n\n    const rawTemplate = root.querySelector(\"[data-dpp-raw]\");\n    const panel = root.querySelector(\"[data-dpp-panel]\");\n    const playerEl = root.querySelector(\"[data-dpp-player]\");\n    const groupsEl = root.querySelector(\"[data-dpp-groups]\");\n    if (!rawTemplate || !panel || !playerEl || !groupsEl) return;\n\n    const source = normalizeRaw(rawTemplate.innerHTML || rawTemplate.textContent || \"\");\n    const playerInput = extractTag(source, \"player_input\") || \"未提供玩家输入\";\n    const tagMap = {};\n    TABS.flatMap((tab) => tab.tags).forEach((tagName) => {\n      tagMap[tagName] = extractTag(source, tagName);\n    });\n\n    playerEl.textContent = playerInput;\n    groupsEl.innerHTML = renderTabs(tagMap);\n    groupsEl.addEventListener(\"click\", (event) => {\n      const entryButton = event.target && event.target.closest && event.target.closest(\"[data-dpp-audit-entry]\");\n      if (entryButton && groupsEl.contains(entryButton)) {\n        toggleAuditBody(groupsEl);\n        return;\n      }\n\n      const amButton = event.target && event.target.closest && event.target.closest(\"[data-dpp-am-code]\");\n      if (amButton && groupsEl.contains(amButton)) {\n        loadMemo(groupsEl, amButton.getAttribute(\"data-dpp-am-code\"));\n        return;\n      }\n\n      const button = event.target && event.target.closest && event.target.closest(\"[data-dpp-tab]\");\n      if (!button || !groupsEl.contains(button)) return;\n      toggleTab(groupsEl, button.getAttribute(\"data-dpp-tab\"));\n    });\n    panel.hidden = false;\n  }\n\n  function mountAll() {\n    document.querySelectorAll(\"[data-dpp-root]\").forEach(mount);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", mountAll, { once: true });\n  } else {\n    mountAll();\n  }\n})();\n\n</script>\n</body>\n</html>\n\n```";
  const RAW_CAPTURE = {"placeholder":"$1","targetSelector":"[data-dpp-raw]"};

  function stripHtmlFence(value) {
    let text = String(value || "");
    const fenceIndex = text.toLowerCase().indexOf("```html");
    if (fenceIndex > 0) text = text.slice(fenceIndex);
    if (text.slice(0, 7).toLowerCase() === "```html") {
      text = text.slice(7);
      if (text.slice(0, 2) === "\r\n") text = text.slice(2);
      else if (text.charAt(0) === "\n") text = text.slice(1);
    }
    const trimmed = text.trimEnd();
    return trimmed.endsWith("```") ? trimmed.slice(0, -3) : text;
  }

  function findRoot() {
    return document.querySelector('[data-dlgithub-root="' + ROOT_ID + '"]:not([data-dlgithub-mounted])') ||
      document.querySelector('[data-dlgithub-root="' + ROOT_ID + '"]');
  }

  function copyAttributes(from, to) {
    for (const attr of Array.from(from.attributes || [])) {
      to.setAttribute(attr.name, attr.value);
    }
  }

  function appendImportedNode(parent, sourceNode) {
    if (sourceNode.nodeType !== Node.ELEMENT_NODE || sourceNode.tagName.toLowerCase() !== "script") {
      parent.appendChild(document.importNode(sourceNode, true));
      return;
    }

    const script = document.createElement("script");
    copyAttributes(sourceNode, script);
    script.textContent = sourceNode.textContent || "";
    parent.appendChild(script);
  }

  function collectNodes(doc) {
    const nodes = [];
    for (const node of Array.from(doc.head.childNodes)) {
      if (node.nodeType !== Node.ELEMENT_NODE) continue;
      const tag = node.tagName.toLowerCase();
      if (tag === "style" || tag === "link" || tag === "script") nodes.push(node);
    }
    for (const node of Array.from(doc.body.childNodes)) {
      nodes.push(node);
    }
    return nodes;
  }

  function getRawCapture(root) {
    if (!root) return undefined;
    const node = root.querySelector("[data-dlgithub-raw]");
    return node ? node.innerHTML || node.textContent || "" : undefined;
  }

  function applyRawCapture(parsed, rawText) {
    if (rawText == null) return;
    const target = parsed.querySelector(RAW_CAPTURE.targetSelector);
    if (!target) return;
    if (target.tagName && target.tagName.toLowerCase() === "template" && target.content) {
      target.innerHTML = "";
      target.content.appendChild(parsed.createTextNode(rawText));
      return;
    }
    target.textContent = rawText;
  }

  function showError(root, error) {
    if (!root) return;
    root.dataset.state = "error";
    root.textContent = "Locked regex load failed: " + (error && error.message ? error.message : String(error));
  }

  async function mount(root = findRoot()) {
    if (!root || root.dataset.dlgithubMounted === "1") return;
    root.dataset.dlgithubMounted = "1";
    root.dataset.state = "loading";
    const rawText = getRawCapture(root);

    const html = stripHtmlFence(REPLACE_STRING);
    if (!html.includes("<!DOCTYPE html") && !html.includes("<!doctype html")) {
      throw new Error(MODULE_NAME + " HTML is incomplete");
    }

    const parsed = new DOMParser().parseFromString(html, "text/html");
    applyRawCapture(parsed, rawText);
    const nodes = collectNodes(parsed);
    root.replaceChildren();
    root.dataset.state = "ready";

    for (const node of nodes) {
      appendImportedNode(root, node);
    }
  }

  const root = findRoot();
  mount(root).catch((error) => {
    console.error("[Douluo Locked Regex]", MODULE_NAME, error);
    showError(root || findRoot(), error);
  });
})();
